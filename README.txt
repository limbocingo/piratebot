Basic store bot with categories and products in each category, explore the code and use
it how ever you want. May be needed some changes but it works perfectly fine.

To run it you need Python 3.12 or above and `pip install` the `requirements.txt` file.
If you done all of that just run the `main.py` file.
